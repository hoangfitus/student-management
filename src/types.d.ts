export type Student = {
  mssv: string;
  name: string;
  dob: string;
  gender: string;
  faculty: string;
  course: string;
  program: string;
  address: string;
  email: string;
  phone: string;
  status: string;
};

export type Category = {
  id: number;
  name: string;
};

export type ConfigItem = {
  id: number;
  name: string;
  value: string;
};
